version: 2.1

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    machine:
      resource_class: gpu.nvidia.small
      image: ubuntu-2004-cuda-11.4:202110-01
    steps:
      - checkout
      - run:
          name: "Test is cuda is available.."
          command: nvidia-smi
      - run: docker version
      - run: dpkg -l nvidia-container-toolkit
      - run: nvcc --version
