version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/base:edge
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.7
      - run: |
          echo "=========================="
          cat $BASH_ENV
          docker version
          docker compose version
