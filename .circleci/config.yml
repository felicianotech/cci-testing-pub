version: 2.1

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    machine:
      image: ubuntu-2004:202107-01
    steps:
      - checkout
      - run: |
          python --version
          pyenv --version
          pyenv install -v 3.7.2
          pyenv global 3.7.2
          python --version
