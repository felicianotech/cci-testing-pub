version: 2.1

workflows:
  main:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: circleci/android:31
    steps:
      - checkout
      - run: |
          echo $JAVA_HOME
          which javac
          which java
          jq --version
          docker --version
          node --version
          java -version
          gcloud --version

          echo "======================================================="
          echo "======================================================="
          cat /opt/android/sdk/platform-tools/source.properties
