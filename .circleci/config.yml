version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/go:1.17
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: "Do all the things!"
          command: |
            echo $SHELL
            echo $BASH_ENV
            echo "================================"
            docker run -it cimg/base ls -lah /var/lib/docker/
