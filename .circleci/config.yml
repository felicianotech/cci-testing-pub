version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/go:1.19.11
    steps:
      - checkout
      - run: go version
      - run: echo 'TESTOR=chicken' >> ~/.bashrc
      - setup_remote_docker:
          version: 20.10.14
      - run: docker --version | cut -d' ' -f3 | cut -d',' -f1
      - run: lsb_release -cs
      - run: |
          echo "=========================="
