version: 2.1

workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/go:1.16
    steps:
      - checkout
      - setup_remote_docker:
          version: 19.03.12
      - run:
          name: "Do all the things!"
          command: |
            docker version
