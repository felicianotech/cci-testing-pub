version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/go:1.18
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.11
      - run: go version
      - run: |
          echo "=========================="
