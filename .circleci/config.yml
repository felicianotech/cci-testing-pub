version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/python:3.9
    steps:
      - checkout
      - run: |
          which pyenv
          which python
          echo "=========================="
          cat $BASH_ENV
