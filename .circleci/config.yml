version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: cimg/elixir:1.11.4-node
    steps:
      - checkout
      - run: |
          which pyenv
          which python
          echo "=========================="
          cat $BASH_ENV
